info:
  name: Download
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:3000/download/{{task_id}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
