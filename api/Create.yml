info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/create
  body:
    type: json
    data: |-
      {
        "top_down_view": false,
        "swap_players": false,
        "game_speed": 1.0
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("task_id", res.body);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
