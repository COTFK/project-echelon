info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/create
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("task_id", res.body);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
